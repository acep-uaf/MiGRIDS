<?xml version="1.0" encoding="UTF-8"?>
<!-- Project: GBS Tool -->
<!-- Author: Jeremy VanderMeer, jbvandermeer@alaska.edu -->
<!-- Date: September 27, 2017 -->
<!-- License: MIT License (see LICENSE file of this package for more information -->

<!-- This file describes a general electric energy storage (EES). Specific EES might require further children. -->

<!-- Root tag is 'component' -->
<component name = 'component name'>
    <!-- Declaration of inheritance. Parent of this generic EES descriptor should be the interface itself.  -->
    <childOf>esDescriptor</childOf>

    <!-- 'type' is 'sink-source'. -->
    <type value="sink-source"/>

    <!-- Nameplate values -->
    <!-- 'PInMaxPa' is the maximum real power the unit can receive from the grid, should be 0 if source. Units: kW -->
    <PInMaxPa value="0" unit="kW"/>
    <!-- 'QInMax' is the maximum reactive power the unit can receive from the grid, should be 0 if source. Units. kvar -->
    <QInMaxPa value="0" unit="kvar"/>
    <!-- 'POutMaxPa' is the maximum real power the unit can deliver to the grid. Should be 0 if sink. Units: kW -->
    <POutMaxPa value="0" unit="kW"/>
    <!-- 'QOutMaxPa' is the maximum reactive power the unit can deliver to the grid. Should be 0 if sink. Units: kvar -->
    <QOutMaxPa value="0" unit="kvar"/>

    <!-- Grid service capabilities -->
    <!-- 'isVoltageSource' describes the capability of the component to regulate vars. -->
    <isVoltageSource value="TRUE"/>
    <!-- 'isFrequencyReference' describes the units capability to be the frequency reference of the the grid. -->
    <isFrequencyReference value="TRUE"/>
    <!-- 'isLoadFollowing' describes the capability of a unit to ramp up or down to mitigate demand or non-load following -->
    <!-- production induced frequency or voltage issues -->
    <isLoadFollowing value="TRUE"/>
    <!-- 'isCurtailable' describes if the unit can reduce and clamp power output below available resource based on -->
    <!-- a control signal -->
    <isCurtailable value="TRUE"/>

    <!-- Physical connection -->
    <!-- 'isThreePhase' is TRUE if the unit is a three or four-wire three-phase unit. Default value is TRUE. -->
    <isThreePhase value="TRUE"/>
    <!-- 'isDelta' defines if the unit is Delta of Wye connected. Default is TRUE. -->
    <isDelta value="TRUE"/>
    <!-- 'connectionType' value defines how the unit is connected to the grid, i.e., on which phases, if the neutral -->
    <!-- is present, is the neutral grounded, etc. See the documentation for available values. Default value is 0. -->
    <!-- this ALWAYS has to be changed. Units attribute ALWAYS is NONE, it is here for consistency only. -->
    <connectionType value="0" unit="NONE"/>

    <!-- EES specific parameters -->
    <!-- 'lossMap' describes the loss experienced by the energy storage system for each state of power and energy. -->
    <!--  they are described by the tuples 'p' for power, 'e' for the state of charge and 'loss' for the loss. -->
    <!-- Units for power are P.U. of nameplate power capacity. Positive values of power are used for discharging and
    negative values for charging. Units for 'e' are P.U. nameplate energy capacity. It should be between 0 and 1. This
    should include all losses including secondary systems. Units for loss are kW. Default values are data points at -1, 0 and 1 pu power, -->
    <!-- 0 and 1 pu SoC and 0 loss. -->
    <lossMap>
        <p value="-1" unit="pu"/><e value="0" unit="pu"/><loss value="0" unit="kW"/>
        <p value="-1" unit="pu"/><e value="1" unit="pu"/><loss value="0" unit="kW"/>
        <p value="0" unit="pu"/><e value="0" unit="pu"/><loss value="0" unit="kW"/>
        <p value="0" unit="pu"/><e value="1" unit="pu"/><loss value="0" unit="kW"/>
        <p value="1" unit="pu"/><e value="0" unit="pu"/><loss value="0" unit="kW"/>
        <p value="1" unit="pu"/><e value="1" unit="pu"/><loss value="0" unit="kW"/>
    </lossMap>

    <!-- 'faultPower' Is the ammount of fault power that can be supplied by the ees per cycle.  -->
    <!-- Fault current units are in P.U. of nameplate power and are described by the tuple 'p'.  -->
    <!-- Fault current values are defined for the number of cycles they can be sustained, described by the tuple 'c' -->
    <!-- with units '60Hz cycles' (16.7 ms intervals). -->
    <!-- Default is 1 PU at 5 cycles, ie no fault power capability. -->
    <faultPower>
        <c value="5" unit="cycles"/><p value="1" unit="pu"/>
    </faultPower>

    <!-- 'overLoad' Is the ammount of over load that can be supplied by the ees per second.  -->
    <!-- Over load units are in P.U. of nameplate power and are specified by the number of hours they can   -->
    <!-- operate at. It is described by the tuples 't' (time with units seconds) and 'p' (P.U. of nameplate capacity). -->
    <!-- Default is 1 PU for 1 second, ie no over load capability. -->
    <overLoad>
        <t value="1" unit="sec"/><p value="1" unit="pu"/>
    </overLoad>

    <!-- 'loadFactor24Hr' Is the the maximum average loading the ees is rated for in a 24 hour period. -->
    <!-- Units: P.U. of nameplate power capacity. Default is 1.  -->
    <loadFactor24Hr value="1" unit="PU"/>

    <!-- 'heatingPowerRequirement' is the thermal power required to keep the ees warm, if required.
    It is described by two tuples: 'T' is the ambient (outdoor) temperature in Kelvin. 'pt' is the
    thermal power consumption in kW. Default is one data point at 298 degree K of 0 kW.   -->
    <heatingPowerRequirement>
        <T value="298" unit="K"/><pt value="1" unit="kW"/>
    </heatingPowerRequirement>

    <!-- 'chargeRate' is the fraction of power that it would take to fully charge or discharge the ESS that is the
     maximum charge or discharge power. This creates charging and discharging curves that exponentially approach full
     and zero charge. 'value' should be between 0 and 1. Default value is 0.1.  -->
    <chargeRate value="0.1" unit="NONE"/>

</component>
<!-- EOF -->